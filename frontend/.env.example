# Jarvis AIOS Client Environment Variables
# Duplicate this file to .env in production/development staging instances

# Base URL for the FastAPI backend API
VITE_API_URL=http://localhost:8000

# Default Timeout (in milliseconds) for HTTP network requests
VITE_API_TIMEOUT_MS=10000
